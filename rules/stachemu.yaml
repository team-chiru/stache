assert:
   symbol: '#'
   description: It matches the nested sequence and it sets to true the published data key if the match is successful.

match:
   symbol: ''

notassert:
   symbol: '^'
   description: It tests if it matches the nested sequence and in case of success, it sets to false the corresponding data key and it engines all contains rules.

close:
   symbol: '/'

import:
   symbol: '>'

assign:
   symbol: '='
